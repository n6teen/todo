<%= form_with(model: [ todo1 ], data:{ controller:"reset-form", action: "turbo:submit-end->reset-form#reset"}) do |form| %>
  <% if todo1.errors.any? %>
    <div style="color: red"><h2><%= pluralize(todo1.errors.count, "error") %> prohibited this todo from being saved:</h2><ul><% todo1.errors.each do |error| %><li><%= error.full_message %></li><% end %></ul></div>
  <% end %>

  <div><%= form.label :title, "Item ", style: "display: block inline" %><%= form.text_field :title %>
     <%= form.button "add", type: :submit, name: "todo1[status]", value: "incomplete" %>
  </div>
<% end %>